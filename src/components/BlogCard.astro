---
import FormattedDate from "./FormattedDate.astro";

export interface Props {
  url: string;
  title: string;
  pubDate: Date;
  description?: string;
  tags?: string[];
  isFeature?: boolean;
}

const { 
  url, 
  title, 
  pubDate, 
  description, 
  tags, 
  isFeature = false, 
} = Astro.props;

---
<a
  href={url}
  class={`group block border border-gray-800 bg-black hover:border-gray-400 hover:bg-neutral-900 transition-all duration-300 hover:-translate-y-1 p-6 flex flex-col justify-between ${isFeature ? "md:col-span-2" : ""}`}
>
  <div>
    <h2 class="text-gray-200 group-hover:text-white transition-colors duration-300 text-2xl font-bold uppercase tracking-tight mb-2">
      {title}
    </h2>
    <p class="text-gray-500 text-xs mb-4 uppercase tracking-widest">
      <FormattedDate date={pubDate} />
    </p>
    {description && (
      <p class="text-gray-400 text-sm mb-6 leading-relaxed">
        {description}
      </p>
    )}
  </div>
  
  {tags && tags.length > 0 && (
    <div class="flex flex-wrap gap-2 mt-4">
      {tags.map((tag) => (
        <span class="text-xs text-gray-400 uppercase border border-gray-800 px-2 py-1 hover:border-gray-500 hover:bg-neutral-900 transition-colors duration-300 cursor-default">
          {tag}
        </span>  
      ))}
    </div>
  )}
</a>